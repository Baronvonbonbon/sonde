{
  "name": "sonde",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "sonde \u2014 sent into a runtime to report what is there",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "verify": "node tools/verify.mjs",
    "headless": "node tools/run-headless.mjs",
    "diff": "node tools/diff-report.mjs",
    "pair": "node tools/pair-probe.mjs",
    "check-identity": "node tools/check-identity.mjs",
    "deploy": "node tools/deploy.mjs",
    "whois": "node tools/whois.mjs"
  },
  "dependencies": {
    "@parity/product-sdk": "^0.29.0",
    "@parity/product-sdk-host": "^0.21.0",
    "@parity/product-sdk-statement-store": "^0.6.11",
    "@parity/product-sdk-terminal": "^0.9.1",
    "@parity/truapi": "^0.17.0",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
